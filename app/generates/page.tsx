import Diary from "@styles/diary.module.css";
import GenerateGenerates from "@components/generateGenerates";

function Page() {
    return (
        <section className={Diary.containerGenerates}>
            <GenerateGenerates/>
        </section>
    );
}

export default Page;